<h1>PlannedRoutes#show</h1>
<p>Find me in app/views/planned_routes/show.html.erb</p>

<p>planned_routes_show | GET | /planned_routes/show(.:format) | planned_routes#show</p>


<div style="width: 100%; height: 600px; position: relative;"
  data-controller="calculator"
  data-calculator-markers-value="<%= @markers.to_json %>"
  data-calculator-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
  data-calculator-user-id-value="<%= current_user.id %>">
  <div class="m-4 position-absolute bottom-0 end-0 z-3">
    <%= simple_form_for @planned_route do |f| %>
      <%= f.input :name,  as: :hidden, %>
      <%= f.input :start_point, as: :hidden, %>
      <%= f.input :end_point, as: :hidden %>
      <%= f.input :user_id, as: :hidden, input_html: { value: current_user.id } %>
      <%= f.button :submit, class: "btn btn-primary",
          data: {
                  calculator_target: "form",
                  action: "click->calculator#save"
                } %>
  </div>
</div>
