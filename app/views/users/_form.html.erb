<%=  simple_form_for user,
  html: {
    class: user.errors.any? ? "" : "d-none",
    data: {
      edit_account_target: "form"
    }
  } do |f|
%>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :email %>
  <%= f.input :age %>
  <%= f.input :location %>
  <%= f.input :gender %>
  <%= f.input :photo,
    label: false,
    as: :file
  %>
  <%= f.button :submit, 'Update User', class: 'btn btn-primary rounded-4' %>
<% end %>
